interface:
  display_name: "Token Todo"
  short_description: "Choose current-value work within a safe resource envelope"
  brand_color: "#0F766E"
  default_prompt: "Use $token-todo to build a current-value, safe, reversible maintenance plan within my stated resource and time budget."

policy:
  allow_implicit_invocation: false
